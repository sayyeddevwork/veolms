meta {
  name: 02 Change Order
  type: http
  seq: 2
}

patch {
  url: {{baseUrl}}{{apiBase}}/courses/{{courseId}}/sections/{{sectionId}}
  body: json
  auth: inherit
}

body:json {
  {
    "order":2
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
