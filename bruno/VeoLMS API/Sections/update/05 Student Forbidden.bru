meta {
  name: 05 Student Forbidden
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}{{apiBase}}/courses/{{courseId}}/sections/{{unknownSectionId}}
  body: json
  auth: inherit
}

body:json {
  {
    "title":"Updated"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
