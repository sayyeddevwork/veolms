meta {
  name: 03 Invalid Section
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}{{apiBase}}/courses/{{courseId}}/sections/{{sectionId}}
  body: json
  auth: inherit
}

body:json {
  {
    "title":"Updated"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
