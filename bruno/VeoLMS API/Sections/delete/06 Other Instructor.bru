meta {
  name: 06 Other Instructor
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}{{apiBase}}/courses/{{courseId}}/sections/{{sectionId}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
