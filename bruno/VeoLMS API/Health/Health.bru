meta {
  name: Health
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}{{apiBase}}/health
  body: none
  auth: none
}

tests {
  test("Status 200", function () {
    expect(res.status).to.equal(200);
  });
}
