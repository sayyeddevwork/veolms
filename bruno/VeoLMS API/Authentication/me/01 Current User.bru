meta {
  name: 01 Current User
  type: http
}

get {
  url: {{baseUrl}}{{apiBase}}/auth/me
}

headers {
  Content-Type: application/json
}

tests {
  test("Status 200", function () {
    expect(res.status).to.equal(200);
  });
}
