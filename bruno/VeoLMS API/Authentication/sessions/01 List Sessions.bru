meta {
  name: 01 List Sessions
  type: http
}

get {
  url: {{baseUrl}}{{apiBase}}/auth/sessions
}

headers {
  Content-Type: application/json
}

tests {
  test("Status 200", function () {
    expect(res.status).to.equal(200);
  });
}
