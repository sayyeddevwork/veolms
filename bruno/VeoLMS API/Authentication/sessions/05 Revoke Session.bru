meta {
  name: 05 Revoke Session
  type: http
}

post {
  url: {{baseUrl}}{{apiBase}}/auth/sessions
}

headers {
  Content-Type: application/json
}

tests {
  test("Status 200", function () {
    expect(res.status).to.equal(200);
  });
}
