meta {
  name: 03 Logout Current Device
  type: http
}

get {
  url: {{baseUrl}}{{apiBase}}/auth/sessions
}

headers {
  Content-Type: application/json
}

tests {
  test("Status 200", function () {
    expect(res.status).to.equal(200);
  });
}
