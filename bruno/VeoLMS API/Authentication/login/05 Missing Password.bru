meta {
  name: 05 Missing Password
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}{{apiBase}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "test4@example.com",
    "password": ""
  }
}

tests {
   test("Status 400", function () {
      expect(res.status).to.equal(400);
    });
  
}

settings {
  encodeUrl: true
  timeout: 0
}
