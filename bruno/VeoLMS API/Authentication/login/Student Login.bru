meta {
  name: Student Login
  type: http
  seq: 10
}

post {
  url: {{baseUrl}}{{apiBase}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "student@example.com",
    "password": "Student@123"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
