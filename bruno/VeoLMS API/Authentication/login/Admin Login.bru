meta {
  name: Admin Login
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}{{apiBase}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "admin@example.com",
    "password": "Admin@123"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
