meta {
  name: 01 Refresh Success
  type: http
}

post {
  url: {{baseUrl}}{{apiBase}}/auth/refresh
}

headers {
  Content-Type: application/json
}

tests {
  test("Status 200", function () {
    expect(res.status).to.equal(200);
  });
}
