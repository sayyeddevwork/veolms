meta {
  name: 02 Logout Without Login
  type: http
}

post {
  url: {{baseUrl}}{{apiBase}}/auth/logout
}

headers {
  Content-Type: application/json
}

tests {
  test("Status 200", function () {
    expect(res.status).to.equal(200);
  });
}
