meta {
  name: 01 Verify Email
  type: http
}

post {
  url: {{baseUrl}}{{apiBase}}/auth/email-verification
}

headers {
  Content-Type: application/json
}

tests {
  test("Status 200", function () {
    expect(res.status).to.equal(200);
  });
}
