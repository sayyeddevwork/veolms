meta {
  name: 03 Expired Token
  type: http
}

post {
  url: {{baseUrl}}{{apiBase}}/auth/reset-password
}

headers {
  Content-Type: application/json
}

tests {
  test("Status 200", function () {
    expect(res.status).to.equal(200);
  });
}
