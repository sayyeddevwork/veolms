meta {
  name: 06 Missing Name
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}{{apiBase}}/auth/register
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "",
    "email": "student@example.com",
    "password": "Password@123"
  }
}

tests {
  test("Status 400", function () {
      expect(res.status).to.equal(400);
    });
  
}

settings {
  encodeUrl: true
  timeout: 0
}
